### Техобслуживание: Очистка (VACUUM)

**Дата:** 2025-11-08 
**Семестр:** 4 курс 1 полугодие – 7 семестр  
**Группа:** ПИЖ-б-о-22-1  
**Дисциплина:** Администрирование баз данных  
**Студент:** Плугатырев Владислав Алексеевич

## Цель работы
Всестороннее изучение механизмов очистки (VACUUM) в PostgreSQL. Получение
практических навыков управления ручной и автоматической очисткой, анализа работы HOT-обновлений, исследования влияния очистки на размер таблиц и индексов, а также работы с заморозкой версий строк.

## Теоретическая часть
**Очистка (VACUUM):** Удаляет мертвые версии строк (кортежи), ставшие таковыми в результате UPDATE или DELETE. Освобождает место для повторного использования. Не уменьшает физический размер файла таблицы.

**Полная очистка (VACUUM FULL):** Перезаписывает файл таблицы, полностью удаляя мертвые кортежи и уплотняя данные. Уменьшает физический размер файла, но требует эксклюзивной блокировки.

**Автоочистка (AUTOVACUUM):** Фоновый процесс, автоматически выполняющий VACUUM для таблиц по мере накопления мертвых кортежей. HOT-обновления (Heap-Only Tuple): Специальный вид UPDATE, при котором новая версия строки помещается на ту же страницу, что и старая. Это позволяет избежать обновления индексов и повышает производительность.

**Заморозка (FREEZE):** Помечает версии строк как "замороженные", что позволяет предотвратить проблему оборачивания идентификаторов транзакций (XID).


### Модуль 1: Процессы и режимы остановки

#### Задача 1: Поиск процессов

``` zsh
ps aux | grep postgres
```

```
admin            83268   0,9  0,0 411234000   2704   ??  S    14окт25  24:13.95 /opt/homebrew/opt/postgresql@16/bin/postgres -D /opt/homebrew/var/postgresql@16
admin            96229   0,1  0,0 411234704   2400   ??  Ss   28окт25   4:50.46 postgres: autovacuum launcher   
admin            83277   0,0  0,0 411364752   1728   ??  Ss   14окт25   0:00.91 postgres: logical replication launcher   
admin            83275   0,0  0,0 411363728    544   ??  Ss   14окт25   0:04.75 postgres: walwriter   
admin            83273   0,0  0,0 411241872    736   ??  Ss   14окт25   0:04.32 postgres: background writer   
admin            83272   0,0  0,0 411636112    704   ??  Ss   14окт25   0:06.07 postgres: checkpointer   
admin            84011   0,0  0,0 410724112   1472 s006  S+    6:47     0:00.01 grep postgres
```

#### Задача 2: Остановка Fast
``` shell
# Команда `brew services stop` по умолчанию использует режим fast
brew services stop postgresql@16

brew services start postgresql@16

LATEST_LOG=$(ls -t /opt/homebrew/var/postgresql@16/log/*.log | head -1)
echo "Используем лог-файл: $LATEST_LOG"
tail -n 50 "$LATEST_LOG"
grep -i "checkpoint" "$LATEST_LOG"
```

```
Используем лог-файл: /opt/homebrew/var/postgresql@16/log/postgresql-2025-11-08_185432.log
2025-11-08 18:54:32.915 MSK [88481] LOG:  starting PostgreSQL 16.10 (Homebrew) on aarch64-apple-darwin24.4.0, compiled by Apple clang version 17.0.0 (clang-1700.0.13.3), 64-bit
2025-11-08 18:54:32.917 MSK [88481] LOG:  listening on IPv6 address "::1", port 5432
2025-11-08 18:54:32.917 MSK [88481] LOG:  listening on IPv4 address "127.0.0.1", port 5432
2025-11-08 18:54:32.917 MSK [88481] LOG:  listening on Unix socket "/tmp/.s.PGSQL.5432"
2025-11-08 18:54:32.930 MSK [88485] LOG:  database system was shut down at 2025-11-08 18:54:31 MSK
2025-11-08 18:54:32.936 MSK [88481] LOG:  database system is ready to accept connections
2025-11-08 18:54:52.383 MSK [88481] LOG:  received smart shutdown request
2025-11-08 18:54:52.387 MSK [88481] LOG:  background worker "logical replication launcher" (PID 88488) exited with exit code 1
2025-11-08 18:54:52.387 MSK [88483] LOG:  shutting down
2025-11-08 18:54:52.387 MSK [88483] LOG:  checkpoint starting: shutdown immediate
2025-11-08 18:54:52.389 MSK [88483] LOG:  checkpoint complete: wrote 3 buffers (0.0%); 0 WAL file(s) added, 0 removed, 0 recycled; write=0.001 s, sync=0.001 s, total=0.003 s; sync files=2, longest=0.001 s, average=0.001 s; distance=0 kB, estimate=0 kB; lsn=0/31E815C8, redo lsn=0/31E815C8
2025-11-08 18:54:52.394 MSK [88481] LOG:  database system is shut down
2025-11-08 18:54:52.387 MSK [88483] LOG:  checkpoint starting: shutdown immediate
2025-11-08 18:54:52.389 MSK [88483] LOG:  checkpoint complete: wrote 3 buffers (0.0%); 0 WAL file(s) added, 0 removed, 0 recycled; write=0.001 s, sync=0.001 s, total=0.003 s; sync files=2, longest=0.001 s, average=0.001 s; distance=0 kB, estimate=0 kB; lsn=0/31E815C8, redo lsn=0/31E815C8
```


#### Задача 3: Остановка Immediate
``` Shell
/opt/homebrew/opt/postgresql@16/bin/pg_ctl -D /opt/homebrew/var/postgresql@16 stop -m immediate

brew services start postgresql@16

LATEST_LOG=$(ls -t /opt/homebrew/var/postgresql@16/log/*.log | head -1)
echo "Используем лог-файл: $LATEST_LOG"

tail -n 100 "$LATEST_LOG" | grep -A 10 -B 10 -i "recovery\|redo"
```

```
ожидание завершения работы сервера.... готово
сервер остановлен

Используем лог-файл: /opt/homebrew/var/postgresql@16/log/postgresql-2025-11-08_185910.log
2025-11-08 18:59:10.409 MSK [90027] LOG:  starting PostgreSQL 16.10 (Homebrew) on aarch64-apple-darwin24.4.0, compiled by Apple clang version 17.0.0 (clang-1700.0.13.3), 64-bit
2025-11-08 18:59:10.411 MSK [90027] LOG:  listening on IPv6 address "::1", port 5432
2025-11-08 18:59:10.411 MSK [90027] LOG:  listening on IPv4 address "127.0.0.1", port 5432
2025-11-08 18:59:10.411 MSK [90027] LOG:  listening on Unix socket "/tmp/.s.PGSQL.5432"
2025-11-08 18:59:10.416 MSK [90031] LOG:  database system was interrupted; last known up at 2025-11-08 18:59:00 MSK
2025-11-08 18:59:10.546 MSK [90031] LOG:  database system was not properly shut down; automatic recovery in progress
2025-11-08 18:59:10.548 MSK [90031] LOG:  invalid record length at 0/31E81640: expected at least 24, got 0
2025-11-08 18:59:10.548 MSK [90031] LOG:  redo is not required
2025-11-08 18:59:10.550 MSK [90029] LOG:  checkpoint starting: end-of-recovery immediate wait
2025-11-08 18:59:10.551 MSK [90029] LOG:  checkpoint complete: wrote 3 buffers (0.0%); 0 WAL file(s) added, 0 removed, 0 recycled; write=0.001 s, sync=0.001 s, total=0.002 s; sync files=2, longest=0.001 s, average=0.001 s; distance=0 kB, estimate=0 kB; lsn=0/31E81640, redo lsn=0/31E81640
2025-11-08 18:59:10.553 MSK [90027] LOG:  database system is ready to accept connections
```


Выводы:
 **1. Идентификация процессов PostgreSQL**
С помощью команды `ps aux | grep postgres` были успешно идентифицированы ключевые процессы СУБД:
- **`postmaster`** - главный управляющий процесс
- **`checkpointer`** - процесс контрольных точек
- **`background writer`** - фоновый процесс записи
- **`walwriter`** - процесс записи в WAL-журнал

 **2. Сравнение режимов остановки**
Были проведены эксперименты с двумя режимами остановки:
**Режим FAST (чистое завершение):**
- В логе зафиксирована контрольная точка перед завершением: `checkpoint complete`
- Сервер сообщил: `database system is shut down`
- При последующем запуске восстановление не потребовалось
- **Вывод:** Все "грязные" буферы были сброшены на диск, данные приведены в согласованное состояние

**Режим IMMEDIATE (аварийное завершение):**
- В логе зафиксированы критические сообщения:
  - `database system was interrupted`
  - `database system was not properly shut down`
  - `automatic recovery in progress`
- Несмотря на сообщение `redo is not required`, механизм восстановления был активирован
- **Вывод:** Сервер воспринял остановку как сбой и запустил процедуру восстановления

### Модуль 2:  Буферный кеш и контрольные точки

#### Задача 1: Анализ размера

``` sql
CREATE TABLE wal_test (id INT, data TEXT);

INSERT INTO wal_test 
SELECT generate_series(1,10000), 
       md5(random()::text);

SELECT count(*) FROM wal_test;

SELECT current_setting('block_size') AS block_size_bytes;

SELECT pg_relation_size('wal_test') AS table_size_bytes;

SELECT 
    pg_relation_size('wal_test') AS size_bytes,
    pg_relation_size('wal_test') / current_setting('block_size')::int AS size_blocks;
    
CREATE EXTENSION IF NOT EXISTS pg_buffercache;

SELECT 
    COUNT(*) AS buffers_used,
    COUNT(*) * current_setting('block_size')::int AS memory_used_bytes
FROM pg_buffercache 
WHERE relfilenode = pg_relation_filenode('wal_test');
```

```
CREATE TABLE

INSERT 0 10000

 count 
-------
 10000
(1 строка)

 block_size_bytes 
------------------
 8192
(1 строка)

  
 table_size_bytes 
------------------
           688128
(1 строка)

 size_bytes | size_blocks 
------------+-------------
     688128 |          84
(1 строка)

CREATE EXTENSION
 buffers_used | memory_used_bytes 
--------------+-------------------
           88 |            720896
(1 строка)
```

#### Задача 2: Грязные буферы и контрольная точка
``` sql
-- 1. Смотрим общую статистику по буферам
SELECT 
    buffers_clean,
    buffers_backend,
    buffers_backend_fsync,
    buffers_alloc
FROM pg_stat_bgwriter;

-- 2. Смотрим грязные буферы для таблицы ДО контрольной точки
SELECT 
    COUNT(*) AS total_buffers,
    SUM(CASE WHEN isdirty THEN 1 ELSE 0 END) AS dirty_buffers
FROM pg_buffercache 
WHERE relfilenode = pg_relation_filenode('wal_test');

-- 3. Создаем изменения в таблице (генерируем грязные буферы)
UPDATE wal_test SET data = md5(random()::text) WHERE id <= 1000;

-- 4. Проверяем количество грязных буферов ПОСЛЕ изменений
SELECT 
    COUNT(*) AS total_buffers,
    SUM(CASE WHEN isdirty THEN 1 ELSE 0 END) AS dirty_buffers
FROM pg_buffercache 
WHERE relfilenode = pg_relation_filenode('wal_test');

CHECKPOINT;

-- 5. Проверяем количество грязных буферов ПОСЛЕ контрольной точки
SELECT 
    COUNT(*) AS total_buffers,
    SUM(CASE WHEN isdirty THEN 1 ELSE 0 END) AS dirty_buffers
FROM pg_buffercache 
WHERE relfilenode = pg_relation_filenode('wal_test');
```

```
 buffers_clean | buffers_backend | buffers_backend_fsync | buffers_alloc 
---------------+-----------------+-----------------------+---------------
             0 |               0 |                     0 |           366
(1 строка)

 total_buffers | dirty_buffers 
---------------+---------------
            88 |            85
(1 строка)

UPDATE 1000

 total_buffers | dirty_buffers 
---------------+---------------
            96 |            22
(1 строка)

CHECKPOINT

 total_buffers | dirty_buffers 
---------------+---------------
            96 |             0
(1 строка)
```

1. **Механизм контрольных точек** эффективно выполняет свою основную функцию - принудительную запись всех измененных данных из оперативной памяти на диск. После команды `CHECKPOINT` количество грязных буферов для тестовой таблицы уменьшилось до нуля.
2. **Фоновые процессы PostgreSQL** (background writer) работают непрерывно - даже до выполнения контрольной точки часть грязных буферов уже была записана на диск, что объясняет уменьшение их количества после операции UPDATE.
3. **Буферный кеш сохраняет данные** после контрольной точки, но теперь все буферы помечаются как "чистые", поскольку их содержимое синхронизировано с данными на диске.
4. **Контрольная точка ограничивает объем WAL**, необходимый для восстановления, поскольку гарантирует, что все изменения до определенного момента физически записаны на диск, что подтверждает ее критическую роль в обеспечении надежности СУБД.

#### Задача 3: Предварительное чтение (pg_prewarm)
``` sql
CREATE EXTENSION IF NOT EXISTS pg_prewarm;

-- 2. Очищаем кеш от нашей таблицы 
CHECKPOINT;

-- 3. Проверяем, что таблицы нет в кеше
SELECT COUNT(*) AS buffers_in_cache
FROM pg_buffercache 
WHERE relfilenode = pg_relation_filenode('wal_test');

-- 4. Загружаем таблицу в кеш с помощью pg_prewarm
SELECT pg_prewarm(pg_relation_filenode('wal_test'));

-- 5. Проверяем, что таблица загрузилась в кеш
SELECT COUNT(*) AS buffers_in_cache
FROM pg_buffercache 
WHERE relfilenode = pg_relation_filenode('wal_test');

# Перезапускаем сервер
brew services restart postgresql

psql -d postgres

-- 6. Проверяем, осталась ли таблица в кеше после перезапуска
SELECT COUNT(*) AS buffers_after_restart
FROM pg_buffercache 
WHERE relfilenode = pg_relation_filenode('wal_test');
```

```
CREATE EXTENSION

CHECKPOINT

 buffers_in_cache 
------------------
               96
(1 строка)

 pg_prewarm 
------------
         92
(1 строка)

 buffers_in_cache 
------------------
               96
(1 строка)

 buffers_after_restart 
-----------------------
                     0
(1 строка)
```

### Модуль 3:  Журнал предзаписи (WAL)

#### Задача 1: Размер WAL-записей
``` sql
SELECT pg_current_wal_lsn() AS start_lsn;

CREATE TABLE wal_test_2 (
    id SERIAL PRIMARY KEY,
    data TEXT,
    created_at TIMESTAMP DEFAULT now()
);

INSERT INTO wal_test_2 (data) VALUES 
    ('test data 1'),
    ('test data 2'),
    ('test data 3'),
    ('test data 4'),
    ('test data 5');
    
SELECT 
    pg_current_wal_lsn() AS end_lsn,
    pg_size_pretty(pg_current_wal_lsn() - '0/31E81B8'::pg_lsn) AS wal_size_pretty,
    pg_current_wal_lsn() - '0/31E81B8'::pg_lsn AS wal_size_bytes;
```

```
 start_lsn  
------------
 0/320210A8
(1 строка)

CREATE TABLE

INSERT 0 5

  end_lsn   | wal_size_pretty | wal_size_bytes 
------------+-----------------+----------------
 0/320581A8 | 220 kB          |         225536
(1 строка)
```

#### Задача 2: Анализ WAL
``` sql
SELECT 
    pg_relation_size('wal_test_2') AS table_size,
    pg_relation_size('wal_test_2_pkey') AS index_size;
```

```
 table_size | index_size 
------------+------------
       8192 |      16384
(1 строка)
```

**Анализ объема WAL-записей показал, что их размер значительно превышает объем самих данных по следующим причинам:**
1. **Метаданные операций:** WAL содержит не только пользовательские данные, но и служебную информацию - заголовки записей, идентификаторы транзакций, временные метки.
2. **Индексные операции:** При вставке данных в таблицу с первичным ключом создаются дополнительные WAL-записи для поддержания структуры B-дерева индекса (размер индекса: 16KB).
3. **Механизм full_page_writes:** Для обеспечения защиты от частичных записей страниц при сбое питания, PostgreSQL записывает в WAL полные страницы при их первом изменении после контрольной точки.
4. **Системные операции:** Создание таблицы и индексов также генерирует WAL-записи в системных каталогах PostgreSQL.
Таким образом, объем WAL-записей отражает не только пользовательские данные, но и всю служебную информацию, необходимую для гарантированного восстановления целостности базы данных после сбоя.
#### Задача 3: Восстановление после сбоя
``` sql
INSERT INTO wal_test_2 (data) VALUES ('committed data');

BEGIN;
UPDATE wal_test_2 SET data = 'uncommitted data' WHERE id = 1;

\q

SELECT * FROM wal_test_2 WHERE data LIKE '%committed%' OR data LIKE '%uncommitted%';
```

``` Shell
LATEST_LOG=$(ls -t /opt/homebrew/var/postgresql@16/log/*.log | head -1)
tail -n 50 "$LATEST_LOG" | grep -i "recovery\|redo\|interrupted"
```

```
 id |      data      |         created_at         
----+----------------+----------------------------
 39 | committed data | 2025-11-08 19:41:14.472004
(1 строка)

Лог файл: /opt/homebrew/var/postgresql@16/log/postgresql-2025-11-08_194447.log
2025-11-08 19:44:47.320 MSK [93738] LOG:  starting PostgreSQL 16.10 (Homebrew) on aarch64-apple-darwin24.4.0, compiled by Apple clang version 17.0.0 (clang-1700.0.13.3), 64-bit
2025-11-08 19:44:47.322 MSK [93738] LOG:  listening on IPv6 address "::1", port 5432
2025-11-08 19:44:47.322 MSK [93738] LOG:  listening on IPv4 address "127.0.0.1", port 5432
2025-11-08 19:44:47.323 MSK [93738] LOG:  listening on Unix socket "/tmp/.s.PGSQL.5432"
2025-11-08 19:44:47.334 MSK [93742] LOG:  database system was interrupted; last known up at 2025-11-08 19:44:32 MSK
2025-11-08 19:44:47.432 MSK [93742] LOG:  database system was not properly shut down; automatic recovery in progress
2025-11-08 19:44:47.433 MSK [93742] LOG:  invalid record length at 0/32060868: expected at least 24, got 0
2025-11-08 19:44:47.433 MSK [93742] LOG:  redo is not required
2025-11-08 19:44:47.436 MSK [93740] LOG:  checkpoint starting: end-of-recovery immediate wait
2025-11-08 19:44:47.437 MSK [93740] LOG:  checkpoint complete: wrote 3 buffers (0.0%); 0 WAL file(s) added, 0 removed, 0 recycled; write=0.001 s, sync=0.001 s, total=0.002 s; sync files=2, longest=0.001 s, average=0.001 s; distance=0 kB, estimate=0 kB; lsn=0/32060868, redo lsn=0/32060868
2025-11-08 19:44:47.438 MSK [93738] LOG:  database system is ready to accept connections
2025-11-08 19:46:59.032 MSK [93738] LOG:  received smart shutdown request
2025-11-08 19:46:59.035 MSK [93738] LOG:  background worker "logical replication launcher" (PID 93745) exited with exit code 1
2025-11-08 19:46:59.036 MSK [93740] LOG:  shutting down
2025-11-08 19:46:59.036 MSK [93740] LOG:  checkpoint starting: shutdown immediate
2025-11-08 19:46:59.040 MSK [93740] LOG:  checkpoint complete: wrote 0 buffers (0.0%); 0 WAL file(s) added, 0 removed, 0 recycled; write=0.001 s, sync=0.001 s, total=0.005 s; sync files=0, longest=0.000 s, average=0.000 s; distance=0 kB, estimate=0 kB; lsn=0/32060918, redo lsn=0/32060918
2025-11-08 19:46:59.044 MSK [93738] LOG:  database system is shut down
```

### Модуль 4:  Настройка WAL

#### Задача 1:  Влияние full_page_writes

``` sql
SELECT name, setting, unit FROM pg_settings WHERE name = 'full_page_writes';

CREATE TABLE fpw_test (id SERIAL PRIMARY KEY, data TEXT, number INT);

INSERT INTO fpw_test (data, number)
SELECT md5(random()::text), (random()*1000)::int
FROM generate_series(1, 10000);

SELECT pg_current_wal_lsn() AS start_lsn_on;

CHECKPOINT;

UPDATE fpw_test SET number = number + 1 WHERE id <= 5000;
UPDATE fpw_test SET data = md5(random()::text) WHERE id > 5000;
DELETE FROM fpw_test WHERE id % 10 = 0;
INSERT INTO fpw_test (data, number) SELECT md5(random()::text), (random()*1000)::int FROM generate_series(1, 1000);

SELECT 
    pg_current_wal_lsn() - '0/322487F0'::pg_lsn AS wal_bytes_on,
    pg_size_pretty(pg_current_wal_lsn() - '0/322487F0'::pg_lsn) AS wal_pretty_on;
```

```
       name       | setting | unit 
------------------+---------+------
 full_page_writes | on      | 
(1 строка)

CREATE TABLE

INSERT 0 10000

 start_lsn_on 
--------------
 0/322487F0
(1 строка)

CHECKPOINT

UPDATE 5000
UPDATE 5000
DELETE 1000
INSERT 0 1000

 wal_bytes_on | wal_pretty_on 
--------------+---------------
      3938720 | 3846 kB
(1 строка)
```

``` sql
ALTER SYSTEM SET full_page_writes = off;
SELECT pg_reload_conf();

SELECT pg_current_wal_lsn() AS start_lsn_off;
CHECKPOINT;

UPDATE fpw_test SET number = number - 1 WHERE id <= 5000;
UPDATE fpw_test SET data = md5(random()::text) WHERE id > 5000;
DELETE FROM fpw_test WHERE id % 5 = 0;
INSERT INTO fpw_test (data, number) SELECT md5(random()::text), (random()*1000)::int FROM generate_series(1, 1000);

SELECT 
    pg_current_wal_lsn() - '0/3298B160'::pg_lsn AS wal_bytes_off,
    pg_size_pretty(pg_current_wal_lsn() - '0/3298B160'::pg_lsn) AS wal_pretty_off;
```

```
ALTER SYSTEM
 pg_reload_conf 
----------------
 t
(1 строка)

       name       | setting | unit 
------------------+---------+------
 full_page_writes | off     | 
(1 строка)

 start_lsn_off 
---------------
 0/3298B160
(1 строка)

CHECKPOINT

UPDATE 4000
UPDATE 5800
DELETE 200
INSERT 0 1000

 wal_bytes_off | wal_pretty_off 
---------------+----------------
       2514888 | 2456 kB
(1 строка)
```
**Эксперимент с параметром full_page_writes показал значительное влияние на объем генерируемых WAL-записей:**
1. **При full_page_writes = ON** объем WAL составил **3846 kB**, что на **36.1% больше** чем при OFF-режиме.
2. **При full_page_writes = OFF** объем WAL сократился до **2456 kB** за счет записи только измененных частей страниц (дельт).
3. **Причина различия:** При включенном full_page_writes система записывает в WAL полные страницы данных (по 8KB) при их первом изменении после контрольной точки, что обеспечивает защиту от частичных записей при сбое питания, но увеличивает объем журнала.
4. **Компромисс надежности:**
    - **ON**: Выше надежность, защита от corruption при сбоях питания, но больший объем WAL
    - **OFF**: Экономия места на диске и снижение нагрузки I/O, но риск повреждения данных при аппаратных сбоях
#### Задача 2: Эффективность сжатия WAL
``` sql
SELECT name, setting FROM pg_settings WHERE name = 'wal_compression';

-- ест с wal_compression = OFF
ALTER SYSTEM SET wal_compression = off;
SELECT pg_reload_conf();

SELECT pg_current_wal_lsn() AS start_lsn_no_compression;
CHECKPOINT;

UPDATE fpw_test SET data = repeat(md5(random()::text), 10) WHERE id <= 3000;
INSERT INTO fpw_test (data, number) 
SELECT repeat(md5(random()::text), 5), (random()*1000)::int 
FROM generate_series(1, 2000);

SELECT 
    pg_current_wal_lsn() - '0/32BF1180'::pg_lsn AS wal_bytes_no_compression,
    pg_size_pretty(pg_current_wal_lsn() - '0/32BF1180'::pg_lsn) AS wal_pretty_no_compression;

-- Тест с wal_compression = ON
ALTER SYSTEM SET wal_compression = on;
SELECT pg_reload_conf();

SELECT pg_current_wal_lsn() AS start_lsn_with_compression;
CHECKPOINT;

UPDATE fpw_test SET data = repeat(md5(random()::text), 10) WHERE id <= 3000;
INSERT INTO fpw_test (data, number) 
SELECT repeat(md5(random()::text), 5), (random()*1000)::int 
FROM generate_series(1, 2000);

SELECT 
    pg_current_wal_lsn() - '0/32E8B870'::pg_lsn AS wal_bytes_with_compression,
    pg_size_pretty(pg_current_wal_lsn() - '0/32E8B870'::pg_lsn) AS wal_pretty_with_compression;
```

```
ALTER SYSTEM
 pg_reload_conf 
----------------
 t
(1 строка)

 start_lsn_no_compression 
--------------------------
 0/32BF1180
(1 строка)
CHECKPOINT

UPDATE 2400
INSERT 0 2000

 wal_bytes_no_compression | wal_pretty_no_compression 
--------------------------+---------------------------
                  2729712 | 2666 kB
(1 строка)

 pg_reload_conf 
----------------
 t
(1 строка)

 start_lsn_with_compression 
----------------------------
 0/32E8B870
(1 строка)

CHECKPOINT

UPDATE 2400
INSERT 0 2000

 wal_bytes_with_compression | wal_pretty_with_compression 
----------------------------+-----------------------------

                    2508664 | 2450 kB
(1 строка)
```


## Выводы

В ходе лабораторной работы был всесторонне изучен механизм обеспечения надежности в PostgreSQL, основанный на совместной работе **буферного кеша, контрольных точек и журнала предзаписи (WAL)**.

##### **Модуль 1: Процессы и режимы остановки**
- **Установлено**, что PostgreSQL использует архитектуру многопроцессной системы с выделенными процессами для ключевых операций (checkpointer, background writer, walwriter)
- **Экспериментально подтверждена** разница между режимами остановки:
  - **Fast-остановка** выполняется корректно с предварительной контрольной точкой
  - **Immediate-остановка** воспринимается как аварийная и требует восстановления
- **Практически доказана** способность PostgreSQL к автоматическому восстановлению после сбоев

##### **Модуль 2: Буферный кеш и контрольные точки**
- **Исследован** механизм работы буферного кеша как промежуточного звена между оперативной памятью и диском
- **Продемонстрирована** эффективность контрольных точек для принудительной записи "грязных" буферов
- **Проанализированы** ограничения расширения pg_prewarm, работающего только в рамках текущего сеанса сервера
- **Установлено**, что контрольные точки полностью очищают грязные буферы, обеспечивая синхронизацию данных

##### **Модуль 3: Журнал предзаписи (WAL)**
- **Измерен** объем WAL-записей для различных операций
- **Объяснены** причины значительного размера WAL (метаданные, индексные операции, полностраничные записи)
- **Практически доказана** надежность механизма восстановления:
  - Зафиксированные транзакции сохраняются
  - Незафиксированные транзакции откатываются
- **Подтвержден** принцип Write-Ahead Logging как фундамент надежности СУБД

##### **Модуль 4: Настройка WAL**
- **Количественно оценено** влияние параметра `full_page_writes` на объем WAL (разница 36.1%)
- **Выявлен** компромисс между надежностью и производительностью:
  - `full_page_writes = on` обеспечивает защиту от частичных записей
  - `full_page_writes = off` сокращает объем WAL ценой снижения надежности
- **Изучена** эффективность сжатия WAL для оптимизации использования дискового пространства

