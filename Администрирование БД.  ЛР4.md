### Техобслуживание: Очистка (VACUUM)

**Дата:** 2025-10-28 
**Семестр:** 4 курс 1 полугодие – 7 семестр  
**Группа:** ПИЖ-б-о-22-1  
**Дисциплина:** Администрирование баз данных  
**Студент:** Плугатырев Владислав Алексеевич

## Цель работы
Всестороннее изучение механизмов очистки (VACUUM) в PostgreSQL. Получение
практических навыков управления ручной и автоматической очисткой, анализа работы HOT-обновлений, исследования влияния очистки на размер таблиц и индексов, а также работы с заморозкой версий строк.

## Теоретическая часть
**Очистка (VACUUM):** Удаляет мертвые версии строк (кортежи), ставшие таковыми в результате UPDATE или DELETE. Освобождает место для повторного использования. Не уменьшает физический размер файла таблицы.

**Полная очистка (VACUUM FULL):** Перезаписывает файл таблицы, полностью удаляя мертвые кортежи и уплотняя данные. Уменьшает физический размер файла, но требует эксклюзивной блокировки.

**Автоочистка (AUTOVACUUM):** Фоновый процесс, автоматически выполняющий VACUUM для таблиц по мере накопления мертвых кортежей. HOT-обновления (Heap-Only Tuple): Специальный вид UPDATE, при котором новая версия строки помещается на ту же страницу, что и старая. Это позволяет избежать обновления индексов и повышает производительность.

**Заморозка (FREEZE):** Помечает версии строк как "замороженные", что позволяет предотвратить проблему оборачивания идентификаторов транзакций (XID).


### Модуль 1: Ручное очистка и ее влияние

#### Задача 1: Отключение автоочистки

``` sql
ALTER SYSTEM SET autovacuum = off;

SELECT pg_reload_conf();

SHOW autovacuum;
```

```
You're about to run a destructive command.
Do you want to proceed? [y/N]: y
Your call!
ALTER SYSTEM
Time: 0.007s

+----------------+
| pg_reload_conf |
|----------------|
| True           |
+----------------+
SELECT 1
Time: 0.014s


+------------+
| autovacuum |
|------------|
| off        |
+------------+
SHOW 1
Time: 0.009s
```

#### Задача 2: Подготовка данных
``` sql
CREATE TABLE vacuum_test (id INT);
CREATE INDEX idx_vacuum_test ON vacuum_test(id);

INSERT INTO vacuum_test 
SELECT (random() * 100000)::INT 
FROM generate_series(1, 100000);

SELECT 
    pg_size_pretty(pg_relation_size('vacuum_test')) as table_size,
    pg_size_pretty(pg_relation_size('idx_vacuum_test')) as index_size,
    pg_size_pretty(pg_total_relation_size('vacuum_test')) as total_size;
```

```
CREATE TABLE
CREATE INDEX
INSERT 0 100000

+------------+------------+------------+
| table_size | index_size | total_size |
|------------+------------+------------|
| 3544 kB    | 2384 kB    | 5952 kB    |
+------------+------------+------------+
SELECT 1
Time: 0.367s

```


#### Задача 3: Наблюдение без очистки
``` sql
-- Таблица для отслеживания размеров
CREATE TABLE size_monitor (
    iteration INT,
    table_size BIGINT,
    index_size BIGINT,
    total_size BIGINT,
    description TEXT
);

-- Записываем начальный размер
INSERT INTO size_monitor VALUES (0, 
    pg_relation_size('vacuum_test'),
    pg_relation_size('idx_vacuum_test'), 
    pg_total_relation_size('vacuum_test'),
    'После начальной вставки'
);

-- Выполняем несколько обновлений БЕЗ VACUUM
DO $$
BEGIN
    FOR i IN 1..5 LOOP
        -- Каждое обновление создает новые версии строк
        UPDATE vacuum_test SET id = id + 1 WHERE random() < 0.5;
        
        INSERT INTO size_monitor VALUES (i,
            pg_relation_size('vacuum_test'),
            pg_relation_size('idx_vacuum_test'),
            pg_total_relation_size('vacuum_test'),
            'После обновления ' || i || ' без VACUUM'
        );
        
        RAISE NOTICE 'Итерация %: таблица=%, индекс=%', 
            i,
            pg_size_pretty(pg_relation_size('vacuum_test')),
            pg_size_pretty(pg_relation_size('idx_vacuum_test'));
    END LOOP;
END;
$$;
```

При UPDATE создается новая версия строки, а старая становится мертвым кортежем
Мертвые кортежи занимают место, но не видны в запросах

```
CREATE TABLE

INSERT 0 1

  

Итерация 1: таблица=5320 kB, индекс=3552 kB
Итерация 2: таблица=7088 kB, индекс=4472 kB
Итерация 3: таблица=8856 kB, индекс=4752 kB
Итерация 4: таблица=10 MB, индекс=5248 kB
Итерация 5: таблица=12 MB, индекс=5976 kB
DO
Time: 1.233s (1 second), executed in: 1.233s (1 second)
```
#### Задача 4: Полная очистка

 ``` sql
 -- Записываем размер перед VACUUM FULL
INSERT INTO size_monitor VALUES (6,
    pg_relation_size('vacuum_test'),
    pg_relation_size('idx_vacuum_test'),
    pg_total_relation_size('vacuum_test'),
    'Перед VACUUM FULL'
);

-- VACUUM FULL перезаписывает весь файл, уплотняя данные
VACUUM FULL vacuum_test;

-- Записываем размер после VACUUM FULL
INSERT INTO size_monitor VALUES (7,
    pg_relation_size('vacuum_test'),
    pg_relation_size('idx_vacuum_test'),
    pg_total_relation_size('vacuum_test'),
    'После VACUUM FULL'
);

-- Смотрим результаты
SELECT description, 
       pg_size_pretty(table_size) as table_size,
       pg_size_pretty(index_size) as index_size,
       pg_size_pretty(total_size) as total_size
FROM size_monitor 
ORDER BY iteration;
 ```

```
INSERT 0 1
Time: 0.003s


VACUUM
Time: 0.140s

INSERT 0 1
Time: 0.003s

+-------------------------------+------------+------------+------------+
| description                   | table_size | index_size | total_size |
|-------------------------------+------------+------------+------------|
| После начальной вставки       | 3544 kB    | 2384 kB    | 5952 kB    |
| После обновления 1 без VACUUM | 5320 kB    | 3552 kB    | 8896 kB    |
| После обновления 2 без VACUUM | 7088 kB    | 4472 kB    | 11 MB      |
| После обновления 3 без VACUUM | 8856 kB    | 4752 kB    | 13 MB      |
| После обновления 4 без VACUUM | 10 MB      | 5248 kB    | 16 MB      |
| После обновления 5 без VACUUM | 12 MB      | 5976 kB    | 18 MB      |
| Перед VACUUM FULL             | 12 MB      | 5976 kB    | 18 MB      |
| После VACUUM FULL             | 3544 kB    | 1936 kB    | 5480 kB    |
+-------------------------------+------------+------------+------------+
SELECT 8
Time: 0.009s
```

#### Задача 5: Обычная очистка

``` sql
INSERT INTO vacuum_test SELECT (random() * 100000)::INT FROM generate_series(1, 100000);

INSERT INTO size_monitor VALUES (0,
        pg_relation_size('vacuum_test'),
        pg_relation_size('idx_vacuum_test'),
        pg_total_relation_size('vacuum_test'),
        'Начальное состояние');
        
UPDATE vacuum_test SET id = id + 1 WHERE random() < 0.5;
VACUUM vacuum_test;
INSERT INTO size_monitor VALUES (1,
    pg_relation_size('vacuum_test'),
    pg_relation_size('idx_vacuum_test'),
    pg_total_relation_size('vacuum_test'),
    'После обновления ' || 1 || ' с VACUUM'
);

UPDATE vacuum_test SET id = id + 1 WHERE random() < 0.5;
VACUUM vacuum_test;
INSERT INTO size_monitor VALUES (2,
    pg_relation_size('vacuum_test'),
    pg_relation_size('idx_vacuum_test'),
    pg_total_relation_size('vacuum_test'),
    'После обновления ' || 2 || ' с VACUUM'
);

UPDATE vacuum_test SET id = id + 1 WHERE random() < 0.5;
VACUUM vacuum_test;
INSERT INTO size_monitor VALUES (3,
    pg_relation_size('vacuum_test'),
    pg_relation_size('idx_vacuum_test'),
    pg_total_relation_size('vacuum_test'),
    'После обновления ' || 3 || ' с VACUUM'
);

UPDATE vacuum_test SET id = id + 1 WHERE random() < 0.5;
VACUUM vacuum_test;
INSERT INTO size_monitor VALUES (4,
    pg_relation_size('vacuum_test'),
    pg_relation_size('idx_vacuum_test'),
    pg_total_relation_size('vacuum_test'),
    'После обновления ' || 4 || ' с VACUUM'
);

SELECT description, 
       pg_size_pretty(table_size) as table_size,
       pg_size_pretty(index_size) as index_size
FROM size_monitor 
ORDER BY iteration;
```

```
You're about to run a destructive command.
Do you want to proceed? [y/N]: y
Your call!
INSERT 0 100000
INSERT 0 1
UPDATE 49970
VACUUM
INSERT 0 1
UPDATE 49745
VACUUM
INSERT 0 1
UPDATE 50241
VACUUM
INSERT 0 1
UPDATE 49791
VACUUM
INSERT 0 1

+-----------------------------+------------+------------+
| description                 | table_size | index_size |
|-----------------------------+------------+------------|
| Начальное состояние         | 3544 kB    | 2400 kB    |
| После обновления 1 с VACUUM | 5312 kB    | 3568 kB    |
| После обновления 2 с VACUUM | 5312 kB    | 3648 kB    |
| После обновления 3 с VACUUM | 5320 kB    | 3696 kB    |
| После обновления 4 с VACUUM | 5320 kB    | 3696 kB    |
+-----------------------------+------------+------------+
SELECT 5
Time: 1.085s (1 second), executed in: 1.076s (1 second)
```
#### Задача 6: Включение автоочистки
``` sql
ALTER SYSTEM SET autovacuum = on;
SELECT pg_reload_conf();
SHOW autovacuum;
```

```
Do you want to proceed? [y/N]: y
Your call!
ALTER SYSTEM
+----------------+
| pg_reload_conf |
|----------------|
| True           |
+----------------+
SELECT 1
+------------+
| autovacuum |
|------------|
| on         |
+------------+
SHOW 1
Time: 0.022s
```

### Модуль 2:  HOT-обновления и самоочистка

#### Задача 1: Самоочистка без HOT

``` sql
-- Создаем таблицу без индексов
CREATE TABLE no_hot_test (id INT, data TEXT, extra TEXT);

-- Вставляем тестовые данные
INSERT INTO no_hot_test 
SELECT i, 'data_' || i, 'extra_' || i 
FROM generate_series(1, 100) i;

SELECT lp, t_xmin, t_xmax, t_ctid 
FROM heap_page_items(get_raw_page('no_hot_test', 0)) 
WHERE lp <= 10;

UPDATE no_hot_test SET data = 'updated_' || data WHERE id % 2 = 0;

SELECT lp, t_xmin, t_xmax, t_ctid 
FROM heap_page_items(get_raw_page('no_hot_test', 0)) 
WHERE lp <= 10;

CREATE INDEX idx_no_hot_data ON no_hot_test(data);

VACUUM no_hot_test;

SELECT lp, t_xmin, t_xmax, t_ctid 
FROM heap_page_items(get_raw_page('no_hot_test', 0)) 
WHERE lp <= 10;
```

```
CREATE TABLE
INSERT 0 100
+----+--------+--------+--------+
| lp | t_xmin | t_xmax | t_ctid |
|----+--------+--------+--------|
| 1  | 900    | 0      | (0,1)  |
| 2  | 900    | 0      | (0,2)  |
| 3  | 900    | 0      | (0,3)  |
| 4  | 900    | 0      | (0,4)  |
| 5  | 900    | 0      | (0,5)  |
| 6  | 900    | 0      | (0,6)  |
| 7  | 900    | 0      | (0,7)  |
| 8  | 900    | 0      | (0,8)  |
| 9  | 900    | 0      | (0,9)  |
| 10 | 900    | 0      | (0,10) |
+----+--------+--------+--------+
SELECT 10
UPDATE 50
+----+--------+--------+---------+
| lp | t_xmin | t_xmax | t_ctid  |
|----+--------+--------+---------|
| 1  | 900    | 0      | (0,1)   |
| 2  | 900    | 901    | (0,101) |
| 3  | 900    | 0      | (0,3)   |
| 4  | 900    | 901    | (0,102) |
| 5  | 900    | 0      | (0,5)   |
| 6  | 900    | 901    | (0,103) |
| 7  | 900    | 0      | (0,7)   |
| 8  | 900    | 901    | (0,104) |
| 9  | 900    | 0      | (0,9)   |
| 10 | 900    | 901    | (0,105) |
+----+--------+--------+---------+
SELECT 10
CREATE INDEX
VACUUM
+----+--------+--------+--------+
| lp | t_xmin | t_xmax | t_ctid |
|----+--------+--------+--------|
| 1  | 900    | 0      | (0,1)  |
| 2  | <null> | <null> | <null> |
| 3  | 900    | 0      | (0,3)  |
| 4  | <null> | <null> | <null> |
| 5  | 900    | 0      | (0,5)  |
| 6  | <null> | <null> | <null> |
| 7  | 900    | 0      | (0,7)  |
| 8  | <null> | <null> | <null> |
| 9  | 900    | 0      | (0,9)  |
| 10 | <null> | <null> | <null> |
+----+--------+--------+--------+
SELECT 10
Time: 0.049s
```

#### Задача 2: HOT-обновление
``` sql
CREATE TABLE hot_test (id INT PRIMARY KEY, data TEXT, extra TEXT);

CREATE INDEX idx_hot_data ON hot_test(data);

INSERT INTO hot_test VALUES (1, 'initial_data', 'initial_extra');

SELECT lp, t_xmin, t_xmax, t_ctid, t_attrs[3] as extra_attr
FROM heap_page_items(get_raw_page('hot_test', 0));

UPDATE hot_test SET extra = 'updated_extra';

SELECT lp, t_xmin, t_xmax, t_ctid, t_attrs[3] as extra_attr
FROM heap_page_items(get_raw_page('hot_test', 0));
```

```
CREATE TABLE

CREATE INDEX

INSERT 0 1
Time: 0.097s

+----+--------+--------+------------+
| lp | t_xmin | t_xmax | extra_attr |
|----+--------+--------+------------|
| 1  | 906    | 0      | (0,1)      |
+----+--------+--------+------------+
SELECT 1
Time: 0.011s

You're about to run a destructive command.
Do you want to proceed? [y/N]: y 
Your call!
UPDATE 1
Time: 0.004s

+----+--------+--------+------------+
| lp | t_xmin* | t_xmax | extra_attr |
|----+--------+--------+------------|
| 1  | 906    | 907    | (0,2)      |
| 2  | 907    | 0      | (0,2)      |
+----+--------+--------+------------+
SELECT 2
Time: 0.009s
```

#### Задача 3: HOT-обновление с переносом
``` sql
CREATE TABLE hot_test2 (
    id INT PRIMARY KEY, 
    data TEXT, 
    padding TEXT
) WITH (fillfactor=50);  -- Оставляем 50% свободного места на странице

CREATE INDEX idx_hot_test2 ON hot_test2(data);

INSERT INTO hot_test2 
SELECT i, 'data_' || i, rpad('x', 200, 'x') 
FROM generate_series(1, 50) i;

SELECT lp, t_xmin, t_xmax, t_ctid 
FROM heap_page_items(get_raw_page('hot_test2', 0)) 
LIMIT 5;

UPDATE hot_test2 SET padding = rpad('y', 400, 'y') WHERE id = 1;

SELECT lp, t_xmin, t_xmax, t_ctid 
FROM heap_page_items(get_raw_page('hot_test2', 0)) 
LIMIT 5;

SELECT lp, t_xmin, t_xmax, t_ctid 
FROM heap_page_items(get_raw_page('hot_test2', 1)) 
LIMIT 5;
```

```
You're about to run a destructive command.
Do you want to proceed? [y/N]: y
Your call!
CREATE TABLE
CREATE INDEX
INSERT 0 50

+----+--------+--------+--------+
| lp | t_xmin | t_xmax | t_ctid |
|----+--------+--------+--------|
| 1  | 910    | 0      | (0,1)  |
| 2  | 910    | 0      | (0,2)  |
| 3  | 910    | 0      | (0,3)  |
| 4  | 910    | 0      | (0,4)  |
| 5  | 910    | 0      | (0,5)  |
+----+--------+--------+--------+
SELECT 5

UPDATE 1

+----+--------+--------+--------+
| lp | t_xmin | t_xmax | t_ctid |
|----+--------+--------+--------|
| 1  | 910    | 911    | (0,17) |
| 2  | 910    | 0      | (0,2)  |
| 3  | 910    | 0      | (0,3)  |
| 4  | 910    | 0      | (0,4)  |
| 5  | 910    | 0      | (0,5)  |
+----+--------+--------+--------+
SELECT 5

+----+--------+--------+--------+
| lp | t_xmin | t_xmax | t_ctid |
|----+--------+--------+--------|
| 1  | 910    | 0      | (1,1)  |
| 2  | 910    | 0      | (1,2)  |
| 3  | 910    | 0      | (1,3)  |
| 4  | 910    | 0      | (1,4)  |
| 5  | 910    | 0      | (1,5)  |
+----+--------+--------+--------+
SELECT 5

Time: 0.047s
```

### Модуль 3:  Глубокая очистка и параметры

#### Задача 1: Многопроходная очистка индекса
``` sql
SHOW maintenance_work_mem;

SET maintenance_work_mem = '1MB';

CREATE TABLE large_vacuum_test (id INT, data TEXT, padding TEXT);
CREATE INDEX idx_large_vacuum_id ON large_vacuum_test(id);
CREATE INDEX idx_large_vacuum_data ON large_vacuum_test(data);

INSERT INTO large_vacuum_test 
SELECT i, 'initial_data_' || i, rpad('x', 500, 'x')
FROM generate_series(1, 50000) i;

UPDATE large_vacuum_test SET data = 'update1_' || data;
UPDATE large_vacuum_test SET data = 'update2_' || data;
UPDATE large_vacuum_test SET data = 'update3_' || data;

VACUUM (VERBOSE, ANALYZE) large_vacuum_test;

SET maintenance_work_mem = '64MB';
```

```
+----------------------+
| maintenance_work_mem |
|----------------------|
| 64MB                 |
+----------------------+
SHOW 1
SET
CREATE TABLE
CREATE INDEX
CREATE INDEX
INSERT 0 50000
UPDATE 50000
UPDATE 50000
UPDATE 50000

очистка "lab04.public.large_vacuum_test"
запущен 1 параллельный процесс очистки для очистки индекса (планировалось: 1)
закончена очистка "lab04.public.large_vacuum_test": сканирований индекса: 1
страниц удалено: 0, осталось: 14285, просканировано: 14285 (100.00% от общего числа)
версий строк: удалено: 50000, осталось: 50000, «мёртвых», но ещё не подлежащих удалению: 0
XID отсечки удаления: 921, на момент завершения операции он имел возраст: 0 XID
новое значение relfrozenxid: 920, оно продвинулось вперёд от предыдущего значения на 6 XID
замораживание: на страницах таблицы (0, 0.00% от общего числа) заморожено кортежей: 0
сканирование индекса требуется: на страницах таблицы (10714, 75.00% от общего числа) удалено мёртвых идентификато>
индекс "idx_large_vacuum_id": всего страниц: 276, сейчас удалено: 0, удалено на данный момент: 0, свободно: 0
индекс "idx_large_vacuum_data": всего страниц: 2278, сейчас удалено: 1560, удалено на данный момент: 1560, свобод>
средняя скорость чтения: 239.815 МБ/с, средняя скорость записи: 143.955 МБ/с
использование буфера: попаданий: 49623, промахов: 2204, «грязных» записей: 1323
использование WAL: записей: 33505, полных образов страниц: 3, байт: 2865027
нагрузка системы: CPU: пользов.: 0.05 с, система: 0.00 с, прошло: 0.07 с
очистка "lab04.pg_toast.pg_toast_16756"
закончена очистка "lab04.pg_toast.pg_toast_16756": сканирований индекса: 0
страниц удалено: 0, осталось: 0, просканировано: 0 (100.00% от общего числа)
версий строк: удалено: 0, осталось: 0, «мёртвых», но ещё не подлежащих удалению: 0
XID отсечки удаления: 921, на момент завершения операции он имел возраст: 0 XID
новое значение relfrozenxid: 921, оно продвинулось вперёд от предыдущего значения на 7 XID
замораживание: на страницах таблицы (0, 100.00% от общего числа) заморожено кортежей: 0
сканирование индекса не требуется: на страницах таблицы (0, 100.00% от общего числа) удалено мёртвых идентификато>
средняя скорость чтения: 31.125 МБ/с, средняя скорость записи: 31.125 МБ/с
использование буфера: попаданий: 19, промахов: 1, «грязных» записей: 1
использование WAL: записей: 1, полных образов страниц: 0, байт: 188
нагрузка системы: CPU: пользов.: 0.00 с, система: 0.00 с, прошло: 0.00 с
анализируется "public.large_vacuum_test"
"large_vacuum_test": просканировано страниц: 14285 из 14285, они содержат "живых" строк: 50000, "мёртвых" строк: >
VACUUM
SET
Time: 5.946s (5 seconds), executed in: 5.945s (5 seconds)
```

#### Задача 2: Обычная очистка после удаления
``` sql
SELECT pg_size_pretty(pg_total_relation_size('large_vacuum_test')) as size_before_delete;

DELETE FROM large_vacuum_test WHERE random() < 0.9;

SELECT pg_size_pretty(pg_total_relation_size('large_vacuum_test')) as size_after_delete;

VACUUM large_vacuum_test;

SELECT pg_size_pretty(pg_total_relation_size('large_vacuum_test')) as size_after_vacuum;
```

```
+--------------------+
| size_before_delete |
|--------------------|
| 132 MB             |
+--------------------+
SELECT 1

DELETE 45014

+-------------------+
| size_after_delete |
|-------------------|
| 132 MB            |
+-------------------+
SELECT 1
VACUUM

+-------------------+
| size_after_vacuum |
|-------------------|
| 132 MB            |
+-------------------+
SELECT 1
Time: 0.616s
```
#### Задача 3: Полная очистка после удаления
``` sql
TRUNCATE large_vacuum_test;
INSERT INTO large_vacuum_test 
SELECT i, 'data_' || i, rpad('x', 500, 'x')
FROM generate_series(1, 50000) i;

SELECT pg_size_pretty(pg_total_relation_size('large_vacuum_test')) as size_before_delete2;

DELETE FROM large_vacuum_test WHERE random() < 0.9;

SELECT pg_size_pretty(pg_total_relation_size('large_vacuum_test')) as size_after_delete2;

VACUUM FULL large_vacuum_test;

SELECT pg_size_pretty(pg_total_relation_size('large_vacuum_test')) as size_after_vacuum_full;
```

```
TRUNCATE TABLE

INSERT 0 50000

+---------------------+
| size_before_delete2 |
|---------------------|
| 32 MB               |
+---------------------+
SELECT 1

DELETE 44932

+--------------------+
| size_after_delete2 |
|--------------------|
| 32 MB              |
+--------------------+
SELECT 1

VACUUM

+------------------------+
| size_after_vacuum_full |
|------------------------|
| 3208 kB                |
+------------------------+
SELECT 1
```

### Модуль 4:  Автоочистка и заморозка

#### Задача 1:  Настройка автоочистки
``` sql
CREATE TABLE autovacuum_test (
    id SERIAL PRIMARY KEY,
    data TEXT,
    value INTEGER
);

INSERT INTO autovacuum_test (data, value)
SELECT 
    md5(random()::text), 
    (random() * 1000)::integer
FROM generate_series(1, 100000);

ALTER TABLE autovacuum_test SET (autovacuum_vacuum_scale_factor = 0.1);
ALTER TABLE autovacuum_test SET (autovacuum_analyze_scale_factor = 0.05);
```

```
ALTER SYSTEM
ALTER SYSTEM

+----------------+
| pg_reload_conf |
|----------------|
| True           |
+----------------+
SELECT 1

CREATE TABLE

ALTER TABLE
Time: 0.036s
```
#### Задача 2: Нагрузочный тест

``` sql
ALTER SYSTEM SET log_autovacuum_min_duration = 0;
SELECT pg_reload_conf();

SELECT 
    pg_size_pretty(pg_total_relation_size('autovacuum_test')) as total_size,
    pg_size_pretty(pg_relation_size('autovacuum_test')) as table_size,
    pg_size_pretty(pg_indexes_size('autovacuum_test')) as index_size;

CREATE OR REPLACE FUNCTION run_autovacuum_test()
RETURNS void AS $$
DECLARE
    i INTEGER;
BEGIN
    FOR i IN 1..20 LOOP
        UPDATE autovacuum_test 
        SET value = value + 1,
            data = md5(random()::text)
        WHERE random() < 0.05;
        
        PERFORM pg_sleep(3);
        
        RAISE NOTICE 'Итерация % завершена', i;
    END LOOP;
END;
$$ LANGUAGE plpgsql;

SELECT run_autovacuum_test();

SELECT 
    pg_size_pretty(pg_total_relation_size('autovacuum_test')) as total_size,
    pg_size_pretty(pg_relation_size('autovacuum_test')) as table_size,
    pg_size_pretty(pg_indexes_size('autovacuum_test')) as index_size;

SELECT 
    schemaname,
    relname,
    last_autovacuum,
    last_autoanalyze,
    autovacuum_count,
    autoanalyze_count
FROM pg_stat_all_tables 
WHERE relname = 'autovacuum_test';
```

```
ALTER SYSTEM

+----------------+
| **pg_reload_conf** |
|----------------|
| True           |
+----------------+
SELECT 1

+------------+------------+------------+
| **total_size** | **table_size** | **index_size** |
|------------+------------+------------|
| 9728 kB    | 7480 kB    | 2208 kB    |
+------------+------------+------------+
SELECT 1

CREATE FUNCTION
Итерация 1 завершена
Итерация 2 завершена
Итерация 3 завершена
Итерация 4 завершена
Итерация 5 завершена
Итерация 6 завершена
Итерация 7 завершена
Итерация 8 завершена
Итерация 9 завершена
Итерация 10 завершена
Итерация 11 завершена
Итерация 12 завершена
Итерация 13 завершена
Итерация 14 завершена
Итерация 15 завершена
Итерация 16 завершена
Итерация 17 завершена
Итерация 18 завершена
Итерация 19 завершена
Итерация 20 завершена

+---------------------+
| run_autovacuum_test |
|---------------------|
|                     |
+---------------------+
SELECT 1

+------------+------------+------------+
| total_size | table_size | index_size |
|------------+------------+------------|
| 19 MB      | 15 MB      | 4408 kB    |
+------------+------------+------------+
SELECT 1

+------------+-----------------+-------------------------------+-------------------------------+----------------->
| schemaname | relname         | last_autovacuum               | last_autoanalyze              | autovacuum_count>
|------------+-----------------+-------------------------------+-------------------------------+----------------->
| public     | autovacuum_test | 2025-10-30 15:42:54.836375+03 | 2025-10-30 15:42:55.160469+03 | 1               >
+------------+-----------------+-------------------------------+-------------------------------+----------------->
SELECT 1
Time: 61.198s (1 minute 1 second), executed in: 61.192s (1 minute 1 second)
```

#### Задача 3: Заморозка версий
``` sql
-- Сеанс 1
CREATE TABLE frz_test(id int, note text);
```
```sql
-- Сеанс 2
BEGIN ISOLATION LEVEL REPEATABLE READ;
```
```sql
-- Сеанс 1
	BEGIN;
	TRUNCATE frz_test;
	COPY frz_test (id, note)
	FROM STDIN WITH FREEZE;
	1 a
	2 b
	3 c
	\.
	COMMIT;
```
```sql
-- Сеанс 1
SELECT id, xmin, age(xmin) AS age
FROM frz_test ORDER BY id;
```
```sql
-- Сеанс 2
SELECT count(*) FROM frz_test;
SELECT id, xmin, age(xmin) AS age
FROM frz_test ORDER BY id;
COMMIT;
```

```
BEGIN

TRUNCATE TABLE
Вводите данные для копирования, разделяя строки переводом строки.
Закончите ввод строкой '\.' или сигналом EOF.

>> 1    a
>> 2 b
>> 3 c
>> \.
COPY 3

COMMIT

FROM frz_test ORDER BY id;

 id | xmin | age 
----+------+-----
  1 | 1001 |   1
  2 | 1001 |   1
  3 | 1001 |   1
(3 строки)

-- 2 Сеанс
BEGIN
 count 
-------
     3
(1 строка)

 id | xmin | age 
----+------+-----
  1 | 1001 |  -1
  2 | 1001 |  -1
  3 | 1001 |  -1
(3 строки)
COMMIT
```

#### Задача 4: Принудительная очистка заморозки
``` sql
DROP TABLE frz_test;
CREATE TABLE frz_test(id int, note text);
ALTER SYSTEM SET autovacuum_freeze_max_age = '100000';
SELECT pg_reload_conf();
ALTER TABLE frz_test SET (autovacuum_enabled = off);

SELECT 'SELECT txid_current();' FROM generate_series(1,150000);
\gexec


VACUUM (VERBOSE, FREEZE) frz_test;
SELECT relname, age(relfrozenxid)
FROM pg_class
WHERE relname = 'frz_test';
```

```
DROP TABLE

CREATE TABLE

ALTER SYSTEM

 pg_reload_conf 
----------------
 t
(1 row)

ALTER TABLE

ИНФОРМАЦИЯ:  агрессивная очистка "lab04.public.frz_test"
ИНФОРМАЦИЯ:  закончена очистка "lab04.public.frz_test": сканирований индекса: 0
страниц удалено: 0, осталось: 0, просканировано: 0 (100.00% от общего числа)
версий строк: удалено: 0, осталось: 0, «мёртвых», но ещё не подлежащих удалению: 0
XID отсечки удаления: 151005, на момент завершения операции он имел возраст: 0 XID
новое значение relfrozenxid: 151005, оно продвинулось вперёд от предыдущего значения на 150002 XID
замораживание: на страницах таблицы (0, 100.00% от общего числа) заморожено кортежей: 0
сканирование индекса не требуется: на страницах таблицы (0, 100.00% от общего числа) удалено мёртвых идентификаторов элементов: 0
средняя скорость чтения: 0.000 МБ/с, средняя скорость записи: 4.216 МБ/с
использование буфера: попаданий: 4, промахов: 0, «грязных» записей: 1
использование WAL: записей: 1, полных образов страниц: 1, байт: 6741
нагрузка системы: CPU: пользов.: 0.00 с, система: 0.00 с, прошло: 0.00 с
ИНФОРМАЦИЯ:  агрессивная очистка "lab04.pg_toast.pg_toast_16865"
ИНФОРМАЦИЯ:  закончена очистка "lab04.pg_toast.pg_toast_16865": сканирований индекса: 0
страниц удалено: 0, осталось: 0, просканировано: 0 (100.00% от общего числа)
версий строк: удалено: 0, осталось: 0, «мёртвых», но ещё не подлежащих удалению: 0
XID отсечки удаления: 151005, на момент завершения операции он имел возраст: 0 XID
новое значение relfrozenxid: 151005, оно продвинулось вперёд от предыдущего значения на 150002 XID
замораживание: на страницах таблицы (0, 100.00% от общего числа) заморожено кортежей: 0
сканирование индекса не требуется: на страницах таблицы (0, 100.00% от общего числа) удалено мёртвых идентификаторов элементов: 0
средняя скорость чтения: 7.948 МБ/с, средняя скорость записи: 15.895 МБ/с
использование буфера: попаданий: 19, промахов: 1, «грязных» записей: 2
использование WAL: записей: 3, полных образов страниц: 2, байт: 5694
нагрузка системы: CPU: пользов.: 0.00 с, система: 0.00 с, прошло: 0.00 с
VACUUM
 relname  | age 
----------+-----
 frz_test |   0
(1 строка)
```

## Выводы
1. Всесторонне изучил механизмы очистки (VACUUM) в PostgreSQL.
2. Получил практические навыки управления ручной и автоматической очисткой, анализа работы HOT-
обновлений.
3. Исследовал влияния очистки на размер таблиц и индексов, а также работы заморозкой версий строк.